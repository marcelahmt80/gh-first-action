name: First Workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello world!"
      - name: Print goodbye
        run: |
          echo "that's all"
          echo "Done -  bye!"
    
